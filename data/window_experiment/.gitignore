# ADR-074: the raw re-search store is ~1 MB of trial lists and the repo's large-file hook refuses
# it. The committed evidence is the summary the driver writes beside it; the store stays on disk.
*.json
!*_summary.json
